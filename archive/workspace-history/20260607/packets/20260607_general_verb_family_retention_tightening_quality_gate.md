# Quality Gate

- `rtk python -B -m py_compile veil-normalize.py` が通る
- `update / updates` smoke で `今は見送る` が出る
- 同 smoke で `短い review から外す寄り` に落ちる
- `rtk rg` で code / README / design / skills が「複数回でもまず今は見送る」契約でそろう
