# Required Rule Tuning Implementation Plan

## 1. Tasks

1. tuning packet に従って current required rules を再配置する
2. post-write readback を取る
3. `veil-profile-audit.py --level 必須` と全体 audit を実行する
4. tuning execution report を残す
