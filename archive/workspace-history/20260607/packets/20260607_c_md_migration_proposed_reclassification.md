# c.md Proposed Reclassification

## Current Source

`C:\Users\f_tan\.veil\rules\c.md`

## Proposed Placement

### 必須

- `checkpoint` → planning / progression の骨格語
- `close verdict` → close report の中心語
- `current state` → current mainline canonical に現れている高需要語
- `current surface` → governance / entry surface の中核語

### 推奨

- `common asset` → 統一価値はあるが hard gate 中心語ではない

### 観察

- `closeable` → 低頻度
- `common asset promotion` → 文脈依存が強い
- `comparison doc` → 低頻度
- `compress` → 汎用動詞で自然言い換え幅が広い
- `current order` → 文脈依存が強い

## Proposed File Shape

```md
# c

## 必須

- checkpoint → チェックポイント
- close verdict → 完了判定
- current state → 今の状態
- current surface → 今の参照先

## 推奨

- common asset → 共通資産

## 観察

- closeable → 完了可能
- common asset promotion → 共通資産昇格
- comparison doc → 比較文書
- compress → 圧縮
- current order → 今の順番
```
