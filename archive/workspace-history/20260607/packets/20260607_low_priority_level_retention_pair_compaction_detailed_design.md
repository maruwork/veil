# Detailed Design

1. `veil-normalize.py` の low-priority compact branch を対象にする
2. `level 提案: ...` と `保留処理: ...` を削除する
3. `level/保留処理: <level> | <retention>` の 1 行へ置き換える
4. `target: ...` は現行のまま維持する
5. docs / skills / current companion を新契約へ追従させる
