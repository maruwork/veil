# Detailed Design

## Touch Point

- `veil-normalize.py`
  - `print_text_result`
  - `existing-match` grouped source header only

## Change

- `source: {source_file} ({len(source_items)}件)` を `source: {source_file}` へ変更

## Surface Writeback

- docs and skills describe grouped source as source file only
