# Detailed Design

## Code

1. `veil-normalize.py` の `existing-match` compact line で `| 表記ゆれ: ...` を `| variants: ...` へ置換する
2. single-source / grouped-source の両 branch を同じ label にそろえる

## Surface

1. `README.md` の normalize 説明段落を `review:` 契約へ修正する
2. `README.md` / `docs/veil-design.md` / 2 skills の `表記ゆれ` 文言を `variants` 契約へ修正する
3. `index/project-current-work.md` を新 bundle へ切り替える
