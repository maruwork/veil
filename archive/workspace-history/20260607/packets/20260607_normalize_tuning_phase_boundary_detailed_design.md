# Detailed Design

## Current

1. `index/project-current-work.md` の active bundle を phase-boundary bundle へ切り替える
2. `current position` に `normalize` tuning close と未達 handoff を書く
3. `next action` は `capture` 候補抽出 tightening packet を切ることに限定する

## Surface

1. `README.md` に normalize tuning stop line を短く追記する
2. `docs/veil-design.md` に tuning reopen 条件を追記する
