# Quality Gate

- `rtk python -B -m py_compile veil-normalize.py` が通る
- text smoke で `variants:` が出る
- text smoke で `variants/target:` が出ない
- `rtk rg` で code / README / design / skills に `variants/target:` が残らない
