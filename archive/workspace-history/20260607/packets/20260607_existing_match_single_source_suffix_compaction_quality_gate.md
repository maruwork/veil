# existing-match single source suffix compaction quality gate

- packet が先に作成されている
- code change は single-source suffix compaction に限定されている
- grouped source header `c.md:` は untouched
- JSON 契約 unchanged
- docs / skills / current companion が同契約
- compile / smoke / readback が通る
