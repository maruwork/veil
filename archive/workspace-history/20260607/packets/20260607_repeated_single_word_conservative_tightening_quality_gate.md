# Quality Gate

- `rtk python -B -m py_compile veil-normalize.py` が通る
- `request / requests` smoke で `説明語候補` ではなくなる
- 同 smoke で `先に採る候補 / 推奨` から外れる
- `rtk rg` で code / README / design / skills が 2 回出現だけでは上げない契約でそろう
