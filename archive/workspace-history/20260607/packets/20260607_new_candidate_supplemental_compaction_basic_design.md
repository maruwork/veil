# Basic Design

## Intent

reason compact 後も `level 提案理由` が独立行で残っている。level 情報は headline にも出ているため、補足は一行 short label に寄せる。

## Chosen Shape

- `level: 推奨 | 説明語候補で一定頻度があり、まず推奨で寄せる候補`

## Non-Goals

- `表記ゆれ` の削除
- `書き込み候補` の削除
- JSON compact 化

