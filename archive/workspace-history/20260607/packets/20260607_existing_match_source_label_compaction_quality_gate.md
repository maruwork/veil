# existing-match source label compaction quality gate

- packet が先に作成されている
- code change は grouped source header の label compaction に限定されている
- 1 件 source の `| source: <file>` は untouched
- JSON 契約 unchanged
- docs / skills / current companion が同契約
- compile / smoke / readback が通る
