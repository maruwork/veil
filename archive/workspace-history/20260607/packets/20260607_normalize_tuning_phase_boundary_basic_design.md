# Basic Design

## Strategy

- `normalize` tuning の現到達点を箇条書きで固定する
- 停止線を「single-word / phrase の保守化はここで一旦 close」と明記する
- 次 phase を `capture` 候補抽出 tightening に切り替える

## Out Of Scope

- `normalize` runtime の追加調整
- `lint` runtime の追加実装
- `capture` 実装そのもの
