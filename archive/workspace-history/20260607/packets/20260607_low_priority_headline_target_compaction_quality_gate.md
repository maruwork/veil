# Quality Gate

- packet files exist
- change is limited to low-priority text branch
- JSON contract unchanged
- `rtk python -B -m py_compile veil-normalize.py` passes
- text smoke proves headline carries target and `target:` line is gone
- docs / skills / current companion match the new contract
