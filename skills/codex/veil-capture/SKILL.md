---
name: veil-capture
description: 会話からAIが使った英語・造語・略語を検出し、日本語に変換して ~/.veil/rules/ に書き込む。会話後に語彙ルールを更新したいとき、またはテキストを引数として渡して解析したいときに使う。
---

# veil-capture — チャットからVEILルールを抽出

会話テキストを解析し、AIが使った英語・造語・略語を検出して日本語に変換し、`~/.veil/rules/` に書き込む。

## 手順

### 1. 解析対象テキストの決定

- 引数が指定されている場合 → その文字列を解析対象とする
- 指定がない場合 → この会話全体を解析対象とする（ユーザー発言・AI発言の両方を含む）

### 2. 検出

**単語単位ではなく意味単位で拾う。**

#### 候補の優先順位

1. **反復している定型語を優先** — 同じ並びで2回以上出た語・複合語を先に候補にする。1回しか出ない語は後回し。
2. **複合語を単語より優先** — `current state` が存在するなら `current` 単独は登録しない。

#### 最小単位の定義

「意味が崩れない最短のまとまり」を1用語とする。

- `current state` → 1用語（`current` 単独は保留）
- `common asset promotion` → 1用語候補
- `repo` 単独 → 複合語の一部として出ているなら保留

#### 固有名詞 vs 一般概念の切り分け

**除外（固有名詞）：** 製品名・サービス名・組織名・固有の機能名
**候補に残す（一般概念）：** 役割語・状態語・判断語・構造語

#### 複合語の判断基準

- 分けると意味が変わる（`current state` → `state` だけでは別物）
- 文書内で同じ並びで2回以上出現している
- 片方だけ訳すと不自然になる
- その並び自体が1つの用語として機能している

#### 保留条件

1語で取るべきか複合語の一部として取るべきか曖昧なものは登録せず、保留候補として完了報告に記載する。

#### 除外する文脈

- バッククォート（`` ` ``）で囲まれた語 → コード識別子・ファイルパスとして除外
- ダブルクォートで囲まれた語 → 固有値として除外
- `key=value` パターン → 設定値として除外

### 3. 正規化してから推奨表記を決定しファイルへ書き込む

#### 登録前の正規化

同一語かどうかの判定前に以下をそろえる：

- 大文字小文字 → 小文字統一
- 単数・複数形 → 単数形に統一（`states` → `state`）
- ハイフン・アンダースコア → スペースに統一（`current-state` = `current state`）

#### 日本語訳 vs カタカナの判断基準

日本語訳にする：
- 日本語にしても意味が落ちない
- 業務上の判断語（状態・原因・境界など）
- 初見でも意味をすぐ取れる基礎語

カタカナにする：
- 業界でカタカナが定着している
- 日本語訳すると不自然・長くなる
- 操作名・概念名として機能している

優先順位：**日本語で自然なら日本語 → 不自然ならカタカナ → 英語のままは最後の手段**

#### ファイルテンプレート

```markdown
# {letter}

- {term} → {訳語}
```

- ヘッダー `# {letter}` は先頭文字（小文字）
- エントリは `- {元語} → {推奨表記}` の形式で1行1エントリ
- エントリは元語のアルファベット昇順に並べる

#### 書き込み先

| 条件 | ファイル |
|------|---------|
| a〜z で始まる語 | `~/.veil/rules/{先頭文字}.md` |
| 数字・記号で始まる語 | `~/.veil/rules/special.md` |

先頭文字は小文字に統一。

#### 書き込み手順

1. 対象ファイルを読み込む（存在しない場合はテンプレートから新規作成）
2. 既存エントリと **大文字小文字を区別せずに** 元語を比較し、一致する場合はスキップ
3. 新規エントリを追加し、全エントリを元語の昇順（case-insensitive）で並べ替えて上書き保存

### 5. veil-sync を実行

ファイル書き込み後、`veil-sync.py` を実行して同期先ファイルに即時反映する。

`~/.veil/config.json` の `sync_script` キーからパスを取得する。

```bash
# bash / zsh (macOS / Linux)
python "$(python3 -c "import json; print(json.load(open('$HOME/.veil/config.json'))['sync_script'])")"
```

```powershell
# PowerShell (Windows)
python (python -c "import json,os; print(json.load(open(os.path.expanduser('~/.veil/config.json')))['sync_script'])")
```

`config.json` が存在しない、または `sync_script` キーがない場合はユーザーに場所を確認する。
VEILサーバーが起動していない場合は同期をスキップし、その旨を報告する。

### 6. 完了報告

書き込んだ内容と保留候補を表形式で報告する。

**登録済み：**

| 元語 | 推奨表記 | 書き込み先 | 結果 |
|------|---------|----------|------|
| current state | 現在の状態 | `~/.veil/rules/c.md` | 追記 |
| ... | ... | ... | スキップ（既存） |

**保留（単独か複合か曖昧）：**

| 語 | 保留理由 |
|----|---------|
| current | `current state` の一部として出現、単独登録は不要と判断 |
| ... | ... |
| ... | ... | ... | スキップ（既存） |
