# Framework Core

This folder holds the primary frameworks and progression authority for `pj-template`.

- open these first when you need the main path for reading, decomposition, progression, or workflow design
- treat these files as the main framework baseline inside `frameworks/`
