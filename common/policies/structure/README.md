# Policy Structure

This folder holds placement, naming, and reference-boundary rules.

- open these when you need to decide where something belongs or how a file should be named or separated
- use `file-role-consistency.md` when editing a long-lived document without drifting its role
- this is the main structure-governance area inside `policies/`
